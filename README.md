Test
Commands to test at first :

Quick install commands:

```bash
pip install numpy matplotlib pandas torch triton pytest
```

If you have issues or want the latest features and bugfixes, especially for Torch and Triton, upgrade them directly:

```bash
pip install --upgrade torch triton
```
To check what device we are connected to :
```bash
nvidia-smi
```

